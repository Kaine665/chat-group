# ChatGroup 环境变量配置
# 复制此文件为 .env 并修改值

# 数据库密码（必须修改！）
DB_PASSWORD=chatgroup123

# JWT 密钥（必须修改！用于加密用户 token）
# 可以用这个命令生成随机密钥：openssl rand -base64 32
JWT_SECRET=please-change-this-in-production

# 前端暴露的端口（默认 80）
PORT=80
