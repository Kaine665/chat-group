# ChatGroup 开发环境配置
# 用于 docker-compose.dev.yml

# 数据库密码（开发环境，不需要太复杂）
DB_PASSWORD=dev123

# JWT 密钥（开发环境用固定值方便调试）
JWT_SECRET=dev-secret-key-do-not-use-in-production

# 前端暴露的端口（开发环境用 3080，避免和生产的 80 冲突）
PORT=3080

# 后端端口（开发环境暴露出来方便调试）
SERVER_PORT=3000
